data_preparation:
  batch_size: 4
  num_workers: 0

model_building:
  features: [16, 32, 64]

training:
  batch_size: 4             
  epochs: 20                
  learning_rate: 0.001      
  weight_decay: 0.0001      
  lr_scheduler: "CosineAnnealingLR"
